{
  "name": "cooking-glasses-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bounded, authenticated Gemini observation proxy for Cooking Glasses",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file=.env src/index.mjs",
    "start:env": "node src/index.mjs",
    "test": "node --test",
    "check": "node --check src/index.mjs && node --check src/server.mjs && node --check src/gemini.mjs && node --check src/validation.mjs"
  }
}
